[package]
name = "buildinfo-example"
description = "Simple example"
version = "0.1.0"
publish = false

[dependencies]
buildinfo = { path = "../lib" }

[build-dependencies]
buildinfo = { path = "../lib" }
